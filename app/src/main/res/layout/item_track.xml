<?xml version="1.0" encoding="utf-8"?>
<!-- Simplified Track Item for Drawer - Shows minimal info, details in bottom sheet -->
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/trackCard"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="12dp"
    android:layout_marginVertical="3dp"
    app:cardElevation="1dp"
    app:cardCornerRadius="12dp"
    app:cardBackgroundColor="@color/track_item_background"
    app:strokeWidth="0dp"
    android:foreground="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingVertical="12dp"
        android:paddingStart="12dp"
        android:paddingEnd="8dp"
        android:gravity="center_vertical">

        <!-- Left: Track Color Indicator with Direction -->
        <FrameLayout
            android:layout_width="40dp"
            android:layout_height="40dp">

            <!-- Background Circle -->
            <View
                android:id="@+id/vTrackColorBg"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="@drawable/bg_track_indicator" />

            <!-- Direction Arrow -->
            <ImageView
                android:id="@+id/ivTrackDirection"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_navigation"
                app:tint="@android:color/white"
                tools:rotation="45"
                android:contentDescription="Track direction" />

        </FrameLayout>

        <!-- Middle: Track ID + Distance (Minimal Info) -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:layout_marginStart="12dp">

            <!-- Track ID Row with Badges -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <TextView
                    android:id="@+id/tvTrackId"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="15sp"
                    android:textStyle="bold"
                    android:textColor="@android:color/white"
                    tools:text="Track 357" />

                <!-- Locked Icon (instead of text badge) -->
                <ImageView
                    android:id="@+id/ivLockedIcon"
                    android:layout_width="16dp"
                    android:layout_height="16dp"
                    android:layout_marginStart="6dp"
                    android:src="@drawable/ic_lock"
                    app:tint="#2196F3"
                    android:visibility="gone"
                    tools:visibility="visible"
                    android:contentDescription="Locked" />

            </LinearLayout>

            <!-- Distance + Classification (simplified) -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginTop="2dp"
                android:gravity="center_vertical">

                <TextView
                    android:id="@+id/tvTrackDistance"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="13sp"
                    android:textColor="#AAAAAA"
                    tools:text="1.2 km" />

                <View
                    android:layout_width="4dp"
                    android:layout_height="4dp"
                    android:layout_marginHorizontal="8dp"
                    android:background="@drawable/bg_dot_separator" />

                <TextView
                    android:id="@+id/tvClassification"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="12sp"
                    android:textColor="#4CAF50"
                    android:textStyle="bold"
                    tools:text="DRONE" />

            </LinearLayout>

        </LinearLayout>

        <!-- Right: Status Indicator (Active/Stale) -->
        <View
            android:id="@+id/vStatusIndicator"
            android:layout_width="8dp"
            android:layout_height="8dp"
            android:layout_marginEnd="4dp"
            android:background="@drawable/bg_status_dot_green"
            tools:background="@drawable/bg_status_dot_red" />

    </LinearLayout>

    <!-- Selection Highlight Border (shown when selected) -->
    <View
        android:id="@+id/vSelectionHighlight"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_track_selected"
        android:visibility="gone"
        tools:visibility="visible" />

</com.google.android.material.card.MaterialCardView>
